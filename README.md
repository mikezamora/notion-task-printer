# notion-task-printer

path to config windows: `C:\Users\mzamora\AppData\Local\python-escpos\python-escpos\config.yaml)`

install `python -m pip install libusb`
move `libusb-1.0.dll` to `C:\Windows\System32\`
